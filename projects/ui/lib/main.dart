import 'package:flutter/material.dart';

var x = new Center(
      child: new Text(
        'What I  do now',
        textDirection: TextDirection.ltr,
      ),
    );

void main() {
  runApp(
    x
  );
}