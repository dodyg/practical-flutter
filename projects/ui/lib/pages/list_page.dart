import "package:flutter/material.dart";

class ListPage extends StatelessWidget{
  
  List<String> _names = [];
  
  ListPage(this._names);

  @override
  Widget build(BuildContext context) {
    
  }
}